<UserControl
    x:Class="AppUserControl.ProductViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppUserControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" Width="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image Source="Assets/bg_box1_S.png" Grid.Row="0" Grid.Column="0" Stretch="None" />
        <Image Source="Assets/btn_product_info_divider.png" Grid.Row="1" Grid.Column="0" Stretch="None" />
        <ListView Grid.Row="2" Grid.Column="0" Foreground="Black">
            <ListViewItem Width="300" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">價錢:100萬</ListViewItem>
            <ListViewItem Width="300" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">內容:無敵海景佛跳牆</ListViewItem>
        </ListView>
    </Grid>
</UserControl>